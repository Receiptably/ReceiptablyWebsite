import React from 'react';

export default function HomePage() {
    // Build UI
    return (
        <React.Fragment>
            <p>This will eventually be the home page once I do some config</p>
        </React.Fragment>
    )
}